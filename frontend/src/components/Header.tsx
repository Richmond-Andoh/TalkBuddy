import React from 'react'
import { AppBar, Toolbar } from '@mui/material'
const Header = () => {
  return (
    <AppBar sx={{postion: "static", }}>
        <Toolbar>
          
        </Toolbar>
    </AppBar>
  )
}

export default Header
